export interface SpreadSheetDataType {
  date: string;
  department: string;
  detail: string;
  eventName: string;
  place: string;
  timeStamp: string;
}

export interface NewsLineType {
  title?: string;
  detail?: string;
}
